# Dogfood Evidence — v0.1.0

Date: 2026-05-04
Tester: automated subagent (Phase 10 runner)
Scratch project: `/var/folders/lz/gqhgwb_j24150h_69nq20rnr0000gn/T/sm-dogfood.Cqxf2ksCvR`
Plugin root: `/Users/liangwushang/Documents/StudyPlan/GT/claude/claudeskill/super-manus`

The v0.1 release plan defines six dogfood smoke checks. Steps 1, 4, 5 touch only
the file system / hook payloads and were automated here. Steps 2, 3, 6 require
fresh interactive Claude Code sessions and remain pending user verification.

## Step 1 — `/super-manus:start dogfood-1` creates the feature folder — PASS

Invocation:

```
SUPER_MANUS_ROOT=/Users/liangwushang/Documents/StudyPlan/GT/claude/claudeskill/super-manus \
  bash $SUPER_MANUS_ROOT/scripts/sm-start.sh dogfood-1
```

Stdout: `<scratch>/docs/super-manus/2026-05-04-dogfood-1`

Verified:
- `docs/super-manus/2026-05-04-dogfood-1/` created in scratch project
- `task_plan.md`, `findings.md`, `progress.md` all present (357 / 281 / 430 bytes)
- Each template has `<feature title>` substituted to `dogfood-1` (verified in `# Task Plan: dogfood-1`, `# Findings: dogfood-1`, `# Progress: dogfood-1` headings)
- `.super-manus/active` contains exactly `2026-05-04-dogfood-1`

## Step 2 — `/super-manus:catchup` after `/clear` references plan unprompted — PENDING USER

Requires a fresh Claude Code session in the scratch project. To verify:

```
cd /var/folders/lz/gqhgwb_j24150h_69nq20rnr0000gn/T/sm-dogfood.Cqxf2ksCvR
claude   # then: /clear, then: /super-manus:catchup
```

Expect the agent to read `docs/super-manus/2026-05-04-dogfood-1/{task_plan,progress}.md` and summarise current phase + outstanding work without being told the path.

## Step 3 — `/super-manus:catchup` mid-session works — PENDING USER

Same scratch dir; in any Claude Code session run `/super-manus:catchup` and confirm the
SessionStart-style injection (or skill-driven re-read) surfaces the latest
`progress.md` state.

## Step 4 — post-commit hook prompts progress.md update — PASS

Setup:

```
cd $SCRATCH
echo "phase 1 work" > work.md
git add work.md && git commit -qm "feat: phase 1 work"
```

Then synthesised the PostToolUse JSON payload Claude Code would send and piped
it into the hook:

```
echo "$PAYLOAD" | bash $PLUGIN/hooks/post-commit.sh
```

Output (single-line JSON, pretty-printed here):

```json
{
  "hookSpecificOutput": {
    "hookEventName": "PostToolUse",
    "additionalContext": "A `git commit` just succeeded. Per the using-sm skill: append a one-line entry to `docs/super-manus/2026-05-04-dogfood-1/progress.md` under `## Completed commits`. ..."
  }
}
```

Verified:
- `hookSpecificOutput.hookEventName == "PostToolUse"` (matches Phase 7 spec)
- `additionalContext` mentions `progress.md` and `Completed commits`
- Active feature folder name `2026-05-04-dogfood-1` correctly resolved from `.super-manus/active`

## Step 5 — session-end hook prompts session log — PASS

Invocation:

```
bash $PLUGIN/hooks/session-end.sh
```

Output:

```json
{
  "hookSpecificOutput": {
    "hookEventName": "Stop",
    "additionalContext": "Session ending. Before stopping, write a session log entry to `docs/super-manus/2026-05-04-dogfood-1/progress.md`. Steps: 1. **Re-read** `progress.md ## Completed commits` ... 3. Append a new entry to `progress.md ## Session log` ..."
  }
}
```

Verified `additionalContext` mentions `Session log`, `Completed commits`, and `Re-read`.

## Step 6 — next-day session resumes cleanly — PENDING USER

Cannot automate — requires reopening Claude Code in the scratch project on a
later calendar day and confirming SessionStart picks up the right active feature
and surfaces the latest `progress.md` state.

## Summary

| Step | Description                              | Status                |
|------|------------------------------------------|-----------------------|
| 1    | `/super-manus:start` creates folder + active file | PASS (automated)      |
| 2    | `/super-manus:catchup` after `/clear`             | PENDING USER          |
| 3    | `/super-manus:catchup` mid-session                | PENDING USER          |
| 4    | post-commit hook reminder                | PASS (automated)      |
| 5    | session-end hook reminder                | PASS (automated)      |
| 6    | next-day resume                          | PENDING USER          |

All automated portions PASS. Three interactive checks remain for the user.
Scratch project left in place at the path above for the user's manual runs;
delete with `rm -rf` when done.
