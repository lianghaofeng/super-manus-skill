<!-- roadmap.md: feature-level module status table. Auto-managed by /super-manus:start, /sync, /prd-add-module, /impl. The agent flips Status from not-started → iterating on first /impl, and iterating → stable when an update's task_plan.md has all phases closed. The Note column is user-owned — the agent should not overwrite a user-written note. Status legend: not-started / iterating / stable / blocked. Headings are stable. -->
# Roadmap

## Modules

| Module | Status | Note |
| --- | --- | --- |
| <module-a> | not-started | |
