<!-- prd_drift.md: append-only log of PRD ↔ implementation conflicts. One row per drift event. Resolution column filled in after the user decides (revert implementation, or run /super-manus:prd-update <module>). The agent must NOT silently update PRD; drift is always logged here first. Headings are stable. -->
# PRD drift log

| When | Module | Conflict | Resolution |
| --- | --- | --- | --- |
